1CT "[X]" const
2CT "[ ]" const
3CT 7 const

3CT 0 > if
    1 3CT <= while
        swap
        1 over <= while
            = if
                1CT .
            else
                2CT .
            end
            swap
            1 +
            swap
        end
        drop drop
        nl
        1 +
        swap
    end
end 